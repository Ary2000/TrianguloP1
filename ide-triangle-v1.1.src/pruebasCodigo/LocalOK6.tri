
let

  local
    const a ~ 1;
    
    local
      const b ~ a + 1 ! a visible aquí
    in
      const c ~ b + a ! c = 3
    end
  in
    const d ~ a + c ! d = 4
  end

in

  putint(d) ! d visible aquí­, imprime 4

end