repeat for x in [0, 1, 2, 4, 8, 16] do 
    putint (x); ! x es un entero del arreglo
    let 
        var x := 'a' ! variable inicializada local, no es la variable de iteración
    in
        put(x) ! x es un caracter
    end
end

! OK
! imprime 0a1a2a4a8a16a