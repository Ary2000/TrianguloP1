let
	var i : Char;
	var j : Integer; 
	const k ~ 8;
	type dia ~ Integer ; ! el ; separa declaraciones, no las termina
      ! hubo ;  pero no hay declaración
in ! in no puede iniciar una declaración
	put(i); 
	put(i) 
end


